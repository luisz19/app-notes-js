const Header = ( ) => {
    return(
        <header className="header">

            <button>menu</button>
            <input type="text" placeholder="pesquisar" />

        </header>

    )
}

export default Header